# yes-no-button

A minimal static web app that presents two buttons — **Yes** and **No** — each playing a corresponding audio clip when clicked.

## Usage

Open `index.html` directly in any browser. No build step or server required.

## Stack

- Vanilla HTML / CSS / JavaScript
- Audio files bundled in `Audio/`
